# Bắt buộc — app hard-fail nếu thiếu. Sinh giá trị thật: openssl rand -hex 32
JWT_SECRET_KEY=

# Dev-only endpoints (/api/dev/reset, run_migrations, regenerate-quests, test-xp).
# Để trống / off trên server prod. Đặt true chỉ trên môi trường dev tin cậy.
ENABLE_DEV_ENDPOINTS=

DATABASE_URL=mysql+pymysql://ielts_user:ielts_password@mysql:3306/ielts_quest
APP_START_DATE=2026-06-04
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
